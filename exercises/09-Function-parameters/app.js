// Your code goes here:
const renderPerson = (name, birthDate, eyesColor, age, gender) => {
  
  return `${name} is a ${age} years old ${gender} born in ${birthDate} with ${eyesColor} `
}

  ///*** Do not edit below this line ****/
  console.log(renderPerson('Bob', '05/22/1983', 'green', 23, 'male'));